name: Greet Contributors

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

jobs:
  greet:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          
          issue-message: |
            👋 Thanks for opening your first issue! We'll review it soon.
            
            **Quick tips:**
            - Check existing issues to avoid duplicates
            - Provide as much detail as possible
            - Be patient - we're working on this in our free time
            
            For questions, use [Discussions](../discussions) instead of issues.
          
          pr-message: |
            🎉 Thanks for your first contribution! Here's what happens next:
            
            1. **Automated checks** will run (may take a few minutes)
            2. **AI review** will comment with feedback
            3. **Maintainers** will review your code
            
            **While you wait:**
            - Make sure all checks pass ✅
            - Address any feedback from the AI reviewer
            - Read our [Contributing Guide](../CONTRIBUTING.md) if you haven't
            
            We appreciate your contribution! 🚀
